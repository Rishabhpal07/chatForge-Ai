{
  "name": "@chatforge/widget",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build && cp dist/widget.js ../web/public/widget.js",
    "dev": "vite build --watch",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "jsdom": "^25.0.1",
    "typescript": "^5",
    "vite": "^5",
    "vitest": "^2.1.8"
  }
}
